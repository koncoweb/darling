# TODO Darling (2026-04-19)

## Bugfix Prioritas

- [ ] Perbaiki request 404 Neon Auth: `GET /get-j-w-t-token`
  - Penyebab paling mungkin: method `getJWTToken` dipanggil pada object Better Auth client proxy, sehingga dipetakan menjadi endpoint `/get-j-w-t-token` (bukan endpoint Neon Auth yang sebenarnya seperti `/token/anonymous`).
  - Target solusi:
    - Jangan menempelkan `getJWTToken` ke object adapter/proxy (hindari `Object.assign(adapter, { getJWTToken: ... })` jika adapter adalah proxy).
    - Pisahkan “API Auth” dan “Token Provider”:
      - Auth client untuk `signIn.email`, `signUp.email`, `getSession`, `signOut`
      - Token getter yang memanggil endpoint yang benar:
        - Anonymous: `{NEON_AUTH_URL}/token/anonymous` (GET)
        - Authenticated: ambil dari session bila tersedia (atau gunakan method internal SDK yang benar, bukan proxy)
    - Pastikan flow web tidak memanggil endpoint yang tidak ada.
  - Lokasi terkait:
    - `lib/neonAuth.ts`
    - `components/auth/AuthProvider.tsx` (fungsi `refreshJwt`)

- [ ] Pastikan semua request Data API selalu mengirim JWT valid
  - Minimal: anonymous JWT untuk akses read-public
  - Authenticated: JWT user setelah login
  - Validasi cepat: panggilan `/videos?select=...` tidak lagi 400 “missing authentication credentials”
  - Lokasi terkait:
    - `lib/dataApi.ts`
    - `app/(tabs)/index.tsx`

- [ ] Normalisasi “public read” vs “authenticated write”
  - Konfirmasi policy RLS sudah sesuai:
    - Read public: `anonymous, authenticated`
    - Write: `authenticated` dengan check ownership
  - Tambahkan fallback UI jika JWT belum tersedia (web/dev)

## Fondasi Produk (MVP)

- [ ] Flow Autentikasi final
  - [ ] Registrasi email+password (Neon Auth) stabil di Android & web preview
  - [ ] Login email+password
  - [ ] Logout + clear token
  - [ ] Persist session dan token (SecureStore di Android, localStorage di web)
  - [ ] Guard routing (opsional): layar tertentu butuh login/role pedagang

- [ ] Role & Profile
  - [ ] Pastikan trigger “user create -> profiles row” berjalan (buyer default)
  - [ ] UI profil menampilkan data user nyata (nama/email) dari Neon Auth
  - [ ] Mekanisme upgrade buyer -> merchant:
    - create `merchants` row untuk user
    - set role di `profiles.role = 'merchant'` (atau metadata sejenis)

## Fitur Beranda (Video Feed)

- [ ] Ganti stub ke feed berbasis data DB
  - [ ] Query list videos + join merchants (sudah ada dasar)
  - [ ] Pagination (range/limit) + infinite scroll
  - [ ] Like video (insert/delete `video_likes`)
  - [ ] Komentar video (insert/select `video_comments`)

- [ ] Integrasi pemutaran video (Android)
  - [ ] Instal `expo-video`
  - [ ] Implement pager feed (pagingEnabled) + preloading/caching sederhana
  - [ ] Fallback thumbnail saat video loading

## Fitur Jelajah (Peta Pedagang)

- [ ] Integrasi peta asli (Android)
  - [ ] Instal `react-native-maps` (Google Maps)
  - [ ] Konfigurasi API key (app config)
  - [ ] Render marker dari `merchants` yang `is_active=true`
  - [ ] Bottom sheet detail pedagang + tombol arahkan

- [ ] Update lokasi pedagang
  - [ ] Integrasi `expo-location`
  - [ ] Toggle “Mulai Keliling” mengaktifkan periodic update:
    - update `last_lat`, `last_lng`, `last_active_at`
  - [ ] Batasi frekuensi update agar hemat baterai

## Fitur Studio (Upload)

- [ ] Upload yang benar (bukan input URL manual)
  - [ ] Integrasi `expo-image-picker` / `expo-camera`
  - [ ] Pilih storage final (Cloudinary/R2/S3) dan flow upload langsung dari client
  - [ ] Setelah upload sukses: simpan `video_url` + `thumbnail_url` + `caption` ke `videos`

- [ ] AI caption (opsional MVP+)
  - [ ] Input nama menu -> generate caption + hashtag (provider AI dipilih kemudian)

## Data & Operasional

- [ ] Seed data (opsional)
  - [ ] Buat 1-3 merchants + videos contoh untuk demo
  - [ ] Pastikan bisa dibaca oleh anonymous role

- [ ] Logging & error handling
  - [ ] Standardisasi pesan error untuk Data API / Auth
  - [ ] Hindari menampilkan raw JSON error ke user (ubah jadi pesan friendly)

## Quality Gate

- [ ] Typecheck selalu hijau (`npx tsc --noEmit`)
- [ ] Web preview tidak crash (auth storage + token)
- [ ] Android run (expo start --android) lancar

